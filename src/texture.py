import pygame

class Texture:
    """(WIP) Class that represents either a pygame Surface or an ID for a GL texture"""
    def __init__(self, surface_or_id: pygame.Surface | int) -> None:
        pass
